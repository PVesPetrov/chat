import React from 'react';
import PropTypes from 'prop-types';

import './Messages.sass';

const Messages = props => {
	return (
		<div>
			<h2>Your message history here</h2>
		</div>
	);
};

Messages.propTypes = {};

export default Messages;
