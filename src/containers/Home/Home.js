import React from 'react';
import PropTypes from 'prop-types';

import './Home.sass';

const Home = props => {
	return (
		<div className='app-home'>
			<h2>Home</h2>
		</div>
	);
};

Home.propTypes = {};

export default Home;
